# 达妙、脉塔与灵足电机控制模式与调参指南

## 1. 文档范围与阅读约定

本文档以当前工程源码为主要依据，说明下列三类电机：

- 达妙 DM4310、DM-J4340-2EC、DM-G6220；
- 脉塔 MYACTUATOR RMD-X4-36 V4；
- 灵足 RobStride RS00、RS01/O1、RS05 和 EL05。

需要特别区分两类信息：

1. **源码可以直接证明的事实**：如参数字段、数值范围、编码方式、软件 PID 计算和当前调用路径。
2. **电机固件内部的闭环规律**：工程内没有附厂家协议 PDF。MIT/Motion 公式按这类协议的字段语义说明；POS、SPD 等固件闭环的内部 PID 细节在本工程中不可见，不应凭源码猜测。

工程的 `MotorTask` 每 1 ms 处理一次电机命令，然后分别调用三套 CAN 发送逻辑，因此主控侧期望更新频率约为 1 kHz。出处：[Task.c:538](./TASK/src/Task.c#L538)、[Task.c:572](./TASK/src/Task.c#L572)、[Task.c:585](./TASK/src/Task.c#L585)。

---

## 2. 通用控制符号与 PID 实现

### 2.1 本文符号

| 符号 | 工程字段 | 含义 | 量纲 |
| --- | --- | --- | --- |
| `q_des` | `Pos` / `position` | 目标位置 | rad |
| `q_actual` | `rx_Pos` | 反馈位置 | rad |
| `w_des` | `W` / `speed` | 目标速度 | rad/s |
| `w_actual` | `rx_W` | 反馈速度 | rad/s |
| `T_ff` | `T` / `torque` | 前馈力矩或直接力矩目标 | N·m |
| `Kp` | `K_P` / `kp` | MIT 位置刚度 | 通常理解为 N·m/rad |
| `Kd` | `K_W` / `kd` | MIT 速度阻尼 | 通常理解为 N·m/(rad/s) |

`K_W` 是电机对象中的旧命名，命令邮箱里对应的字段叫 `kd`，两者是同一个值。达妙的赋值出处：[Task.c:434](./TASK/src/Task.c#L434)；灵足的赋值出处：[Task.c:517](./TASK/src/Task.c#L517)。

### 2.2 MIT/Motion 控制公式

MIT/Motion 模式可按下式理解：

```text
T_cmd = Kp * (q_des - q_actual)
      + Kd * (w_des - w_actual)
      + T_ff
```

各项的作用是：

- `Kp * 位置误差`：生成回到目标位置的力矩，决定“硬度”。
- `Kd * 速度误差`：产生阻尼，抑制超调和振荡。
- `torque`：直接叠加的前馈力矩，常用于重力、摩擦或动力学补偿；它不是力矩上限。

达妙驱动将这 5 个量同时写入 MIT 帧，出处：[DM_Motor.c:430](./Motor/src/DM_Motor.c#L430)。脉塔 Motion 模式使用同样的 5 个量，出处：[MYACTUATOR_Motor.c:125](./Motor/src/MYACTUATOR_Motor.c#L125)。灵足将相同的物理量以 16 bit 量化后发送，出处：[RobStride_Motor.c:281](./Motor/src/RobStride_Motor.c#L281)。

### 2.3 工程软件 PID 公式

达妙的两种软件环和灵足的软件环共用 `PID.c`。离散实现为：

```text
error[k] = reference[k] - feedback[k]
P[k]     = Kp * error[k]
I[k]     = clamp(I[k-1] + Ki * error[k], ±LimitIntegral)
D[k]     = Kd * (error[k] - error[k-1])
output   = clamp(P + I + D + Feedforward + Compensate, ±LimitOutput)
```

也可通过 `Derivative_On_Measurement=true` 使 D 项改为对测量值微分，减少目标阶跃造成的 D 项冲击。D 项还支持 `D_Filter_Alpha` 一阶低通滤波。出处：[PID.h:20](./Function/inc/PID.h#L20)、[PID.c:55](./Function/src/PID.c#L55)、[PID.c:132](./Function/src/PID.c#L132)、[PID.c:142](./Function/src/PID.c#L142)、[PID.c:167](./Function/src/PID.c#L167)。

`Pid_Regulate_Auto()` 会根据 `BasePeriodMs` 和 `ActualPeriodMs` 缩放 Ki/Kd。两者为 0 时都回退到 10 ms，而本工程电机任务是 1 ms，因此使用软件环时应显式设置周期。出处：[PID.h:14](./Function/inc/PID.h#L14)、[PID.c:25](./Function/src/PID.c#L25)。

---

## 3. 达妙电机

### 3.1 当前工程状态

达妙命令邮箱默认模式是 `DAMIAO_CTRL_MIT`，默认型号是 `DM_J4340_2EC`。出处：[Task.c:21](./TASK/src/Task.c#L21)、[damiao_comm_port.h:17](./App/inc/damiao_comm_port.h#L17)。

当前小臂抬升动作使用：

```text
control_mode = DAMIAO_CTRL_MIT
position     = 1.5 rad
speed        = 0 rad/s
torque       = 0 N·m
Kp           = 30
Kd           = 1
```

出处：[Action.c:29](./Function/src/Action.c#L29)、[Action.c:168](./Function/src/Action.c#L168)。

`speed=0` 表示目标速度为零，不是最大速度限制。当电机运动时，`Kd(0-rx_W)` 形成反向阻尼力矩。

### 3.2 参数作用与量纲

| 命令参数 | 对象字段 | 量纲 | 作用 | 源码出处 |
| --- | --- | --- | --- | --- |
| `position` | `Pos` | rad | MIT/POS/PSI 目标位置 | [Task_config.h:70](./TASK/inc/Task_config.h#L70) |
| `speed` | `W` | rad/s | MIT 目标速度；POS 的配套速度字段；SPD 目标速度 | [Task_config.h:71](./TASK/inc/Task_config.h#L71) |
| `torque` | `T` | N·m | MIT 前馈力矩 | [Task_config.h:72](./TASK/inc/Task_config.h#L72) |
| `kp` | `K_P` | 约 N·m/rad | MIT 位置刚度 | [Task_config.h:73](./TASK/inc/Task_config.h#L73) |
| `kd` | `K_W` | 约 N·m/(rad/s) | MIT 速度阻尼 | [Task_config.h:74](./TASK/inc/Task_config.h#L74) |
| `current` | `Current` | A | PSI 模式最大电流 | [Task_config.h:75](./TASK/inc/Task_config.h#L75) |

反馈量为 `rx_Pos`/`rx_W`/`rx_T`/`rx_Tmos`/`rx_Tcoil`，分别表示位置、速度、力矩、MOS 温度和线圈温度。解码出处：[DM_Motor.c:286](./Motor/src/DM_Motor.c#L286)。

### 3.3 型号量化范围

| 型号 | 位置 rad | 速度 rad/s | 力矩映射 N·m | Kp | Kd | 出处 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| DM4310 | -12.5～12.5 | -45～45 | -18～18 | 0～500 | 0～5 | [DM_Motor.h:20](./Motor/inc/DM_Motor.h#L20) |
| DM-G6220 | -12.5～12.5 | -45～45 | -10～10 | 0～500 | 0～5 | [DM_Motor.h:32](./Motor/inc/DM_Motor.h#L32) |
| DM-J4340-2EC | -12.5～12.5 | -8～8 | -28～28 | 0～500 | 0～5 | [DM_Motor.h:62](./Motor/inc/DM_Motor.h#L62) |

上表是协议量化范围，不是机械安全限位，也不是允许持续输出的力矩。G6220 在工程中记录的额定/峰值力矩是 1.3/2.7 N·m，而协议 `TMAX=10 N·m` 只是映射上限。出处：[DM_Motor.h:33](./Motor/inc/DM_Motor.h#L33)、[DM_Motor.h:48](./Motor/inc/DM_Motor.h#L48)。

### 3.4 控制模式与公式

| 模式 | 有效输入 | 控制链/公式 | 源码出处 |
| --- | --- | --- | --- |
| `DAMIAO_CTRL_STOP` | 无 | 应用层发失能命令；驱动 STOP 帧会将 MIT 五个量全置零 | [damiao_comm_port.c:73](./App/src/damiao_comm_port.c#L73)、[DM_Motor.c:498](./Motor/src/DM_Motor.c#L498) |
| `DAMIAO_CTRL_MIT` | Pos、W、T、Kp、Kd | `T_cmd = Kp*(q_des-q_actual) + Kd*(w_des-w_actual) + T_ff` | [DM_Motor.c:430](./Motor/src/DM_Motor.c#L430) |
| `DAMIAO_CTRL_MIT_POS` | Pos、`PID.PosPID`、Kd | STM32：`w_des = PID_pos(q_des-q_actual)`；电机：`T_cmd = Kd*(w_des-w_actual)`。驱动强制 MIT Kp=0、T=0 | [DM_Motor.c:156](./Motor/src/DM_Motor.c#L156)、[DM_Motor.c:443](./Motor/src/DM_Motor.c#L443) |
| `DAMIAO_CTRL_MIT_POS_SPD` | Pos、`PID.PosPID`、`PID.SpeedPID` | `w_des = PID_pos(q_des-q_actual)`，`T = PID_spd(w_des-w_actual)`；电机 MIT Kp=Kd=0 | [DM_Motor.c:164](./Motor/src/DM_Motor.c#L164)、[DM_Motor.c:448](./Motor/src/DM_Motor.c#L448) |
| `DAMIAO_CTRL_POS` | Pos、W | 将 float 位置和速度直接发给电机内部位置模式；内部增益在本工程不可见 | [DM_Motor.c:474](./Motor/src/DM_Motor.c#L474) |
| `DAMIAO_CTRL_SPD` | W | 将 float 速度发给电机内部速度模式 | [DM_Motor.c:481](./Motor/src/DM_Motor.c#L481) |
| `DAMIAO_CTRL_PSI` | Pos、W、Current | 电机内部位置-速度-电流模式，Current 作为最大电流 | [DM_Motor.c:487](./Motor/src/DM_Motor.c#L487) |

G6220 的工程模式表不包含 PSI；DM4310 和 J4340 的模式表包含 PSI。出处：[DM_Motor.c:22](./Motor/src/DM_Motor.c#L22)、[DM_Motor.c:31](./Motor/src/DM_Motor.c#L31)。

### 3.5 原生 MIT 位置控制调参

1. **选对电机型号和映射范围。** 必须保证代码中的 P/V/TMAX 与电机内部设置一致；型号和 ID 只允许在失能状态切换。校验出处：[Task.c:327](./TASK/src/Task.c#L327)、[Task.c:363](./TASK/src/Task.c#L363)。
2. **使能前先对齐当前位置。** 首帧应令 `position=rx_Pos`、`speed=0`、`torque=0`，避免目标阶跃。当前动作会直接发 1.5 rad，STOP→MIT 不会自动进行这个对齐。
3. **从低增益开始。** 空载 G6220 可以把 `Kp=2～5`、`Kd=0.05～0.2`当作保守实验起点，不是厂家推荐值。每次只给 0.01～0.03 rad 的小位置变化。
4. **先升 Kp。** 逐步增大 Kp，直到刚度和响应速度满足需求，在出现持续振荡、撞击或过大力矩峰值前停止。
5. **再升 Kd。** 保持 Kp，增加 Kd 抑制超调。Kd 过大会使响应迟钝、制动冲击增大，并放大速度噪声。
6. **最后加前馈。** 对机械臂可使用重力补偿 `torque=Tg(q)`，以减少靠位置误差产生的长期支撑力矩。
7. **对目标做轨迹限制。** 1 ms 周期下，如果期望目标速度不超过 `v_max`，每周期位置增量应满足 `abs(delta_q) <= v_max * 0.001`。

当前 `Kp=30` 时，0.1 rad 位置误差对应的比例项约为 3 N·m，已超过工程记录的 G6220 2.7 N·m 峰值。当前代码只检查每个 MIT 字段是否在量化范围内，没有对 `Kp*位置误差 + Kd*速度误差 + torque` 的合成力矩做安全限幅。

### 3.6 达妙软件环调参

#### `DAMIAO_CTRL_MIT_POS`

该模式是“STM32 位置 PID + 电机内部速度阻尼”。

1. 必须初始化 `PID.PosPID`，尤其是 `LimitOutput`；达妙初始化会 `memset` 整个对象，默认 `LimitOutput=0` 会导致 PID 输出被夹成零。出处：[DM_Motor.c:128](./Motor/src/DM_Motor.c#L128)、[PID.c:78](./Function/src/PID.c#L78)。
2. 将 `PosPID.LimitOutput` 设为安全的最大目标速度，而不要直接使用协议最大值。
3. 设 `PosPID.Ki=0`、`PosPID.Kd=0`，从小的 `PosPID.Kp` 开始调外环。
4. 命令中 `kd` 必须大于 0，否则电机内部 Kp 被强制为 0、T 也是 0，计算出的目标速度不会产生驱动力矩。
5. 如果有稳态误差，先考虑重力/摩擦补偿，再小幅增加 Ki，并严格限制 `LimitIntegral`。

#### `DAMIAO_CTRL_MIT_POS_SPD`

应按“先内环、后外环”调节：

1. 先固定位置外环输出，调 `SpeedPID`：Ki=0，逐步增大 Kp，再根据需要加小量 Ki；`SpeedPID.LimitOutput` 必须按电机安全力矩设置。
2. 内环稳定后再调 `PosPID`：先 P，再视超调情况增加 D，最后才考虑 I。
3. `PosPID.LimitOutput` 是速度上限，`SpeedPID.LimitOutput` 是力矩上限，两者量纲不同。
4. 两个 PID 都应设置 `ActualPeriodMs=1`，并使 `BasePeriodMs` 与调参时的基准周期一致。

---

## 4. 脉塔 MYACTUATOR RMD-X4-36 V4

### 4.1 当前工程状态和固定参数

脉塔命令邮箱的静态默认值仍是 `MYACTUATOR_CTRL_POSITION`，但当前大臂动作轨迹使用 `MYACTUATOR_CTRL_MOTION`。Motion 初始参数为 `Tff=0 N·m`、`Kp=30`、`Kd=0.5`；启动保持帧使用 `W=0 rad/s`，运行轨迹使用带方向的速度前馈。出处：[myactuator_comm_port.c](./App/src/myactuator_comm_port.c)、[Action.c](./Function/src/Action.c)。

工程中记录的 X4-36 参数为：

| 参数 | 值 | 量纲 | 出处 |
| --- | ---: | --- | --- |
| 力矩常数 | 1.9 | N·m/A | [MYACTUATOR_Motor.h:22](./Motor/inc/MYACTUATOR_Motor.h#L22) |
| 额定力矩 | 10.5 | N·m | [MYACTUATOR_Motor.h:23](./Motor/inc/MYACTUATOR_Motor.h#L23) |
| 峰值力矩 | 34.0 | N·m | [MYACTUATOR_Motor.h:24](./Motor/inc/MYACTUATOR_Motor.h#L24) |

### 4.2 控制参数与量纲

| 参数 | 量纲 | 在不同模式中的作用 | 出处 |
| --- | --- | --- | --- |
| `position` / `Pos` | rad | POSITION 的绝对位置；Motion 的目标位置 | [MYACTUATOR_Motor.h:70](./Motor/inc/MYACTUATOR_Motor.h#L70) |
| `speed` / `W` | rad/s | SPEED 的目标速度；POSITION 的最大速度；Motion 的目标速度 | [Task_config.h:27](./TASK/inc/Task_config.h#L27) |
| `torque` / `T` | N·m | TORQUE 的目标力矩；Motion 的前馈力矩 | [MYACTUATOR_Motor.h:72](./Motor/inc/MYACTUATOR_Motor.h#L72) |
| `kp` / `K_P` | 约 N·m/rad | 仅用于 Motion 位置刚度 | [MYACTUATOR_Motor.c:135](./Motor/src/MYACTUATOR_Motor.c#L135) |
| `kd` / `K_W` | 约 N·m/(rad/s) | 仅用于 Motion 速度阻尼 | [MYACTUATOR_Motor.c:136](./Motor/src/MYACTUATOR_Motor.c#L136) |
| `max_speed` | rad/s | POSITION 模式最大速度 | [MYACTUATOR_Motor.h:75](./Motor/inc/MYACTUATOR_Motor.h#L75) |

### 4.3 控制模式、公式与编码

| 模式 | 命令 | 有效参数 | 控制公式/语义 | 出处 |
| --- | --- | --- | --- | --- |
| `MYACTUATOR_CTRL_STOP` | 0x81 | 无 | 停止电机；应用层从 enabled 切到 disabled 时只发一帧 | [myactuator_comm_port.c:36](./App/src/myactuator_comm_port.c#L36) |
| `MYACTUATOR_CTRL_TORQUE` | 0xA1 | T | `Iq = T / Kt`，其中 `Kt = 1.9 N·m/A`，以 0.01 A/count 发送 | [MYACTUATOR_Motor.c:83](./Motor/src/MYACTUATOR_Motor.c#L83) |
| `MYACTUATOR_CTRL_SPEED` | 0xA2 | W | 将 rad/s 换算为 0.01 deg/s/count，电机固件内部速度环跟踪 | [MYACTUATOR_Motor.c:95](./Motor/src/MYACTUATOR_Motor.c#L95) |
| `MYACTUATOR_CTRL_POSITION` | 0xA4 | Pos、max_speed | 绝对位置按 0.01 deg/count，最大速度按 1 deg/s/count 发送；电机内部位置环跟踪 | [MYACTUATOR_Motor.c:107](./Motor/src/MYACTUATOR_Motor.c#L107) |
| `MYACTUATOR_CTRL_MOTION` | Motion | Pos、W、T、Kp、Kd | `T_cmd = Kp*(q_des-q_actual) + Kd*(w_des-w_actual) + T_ff` | [MYACTUATOR_Motor.c:125](./Motor/src/MYACTUATOR_Motor.c#L125) |

Motion 的量化范围为：Pos ±12.5 rad、W ±45 rad/s、T ±24 N·m、Kp 0～500、Kd 0～5。出处：[MYACTUATOR_Motor.h:26](./Motor/inc/MYACTUATOR_Motor.h#L26)。

任务层对 TORQUE 和 SPEED 的检查上限是根据整数字段能够编码的最大数值计算的，不是电机允许的机械极限。例如代码允许 TORQUE 命令到 ±622.573 N·m，远高于工程记录的 34 N·m 峰值。出处：[Task.c:198](./TASK/src/Task.c#L198)。实际应在业务层另加连续力矩、峰值力矩、速度和温度保护。

### 4.4 脉塔分模式调参方法

#### POSITION 位置模式

该模式的位置 PID 在电机固件里，当前工程没有可以在运行时修改的 Kp/Ki/Kd。可调的外部参数主要是目标位置和 `max_speed`。

1. 首次使能时先令目标位置等于当前多圈位置，验证零点和方向。
2. 从较小的 `max_speed`开始，逐步提高；它不会直接改变内部位置环的刚度。
3. 如果振荡、响应过慢或稳态误差来自固件 PID，需使用厂家调试工具修改内部参数；仅修改本工程 `speed` 不能等效替代调 PID。
4. 当前完成判定只检查位置误差是否小于 0.02 rad，没有速度门限和稳定时间。出处：[Action.c:22](./Function/src/Action.c#L22)、[Action.c:294](./Function/src/Action.c#L294)。

#### TORQUE 力矩模式

1. 此模式没有外部 Kp/Kd，调试量是 `T`。
2. 从额定力矩的小比例开始，对命令做斜坡，不要按任务层的 ±622.573 N·m 数字编码上限调试。
3. 同时观察 `rx_Current`、`rx_T`、`rx_Temp`；反馈力矩按 `rx_Current * 1.9` 估算。出处：[MYACTUATOR_Motor.c:158](./Motor/src/MYACTUATOR_Motor.c#L158)。

#### SPEED 速度模式

1. 固件内部负责速度闭环，工程只给目标 `W`。
2. 从低速开始，对速度目标做加减速斜坡，观察电流和机械负载。
3. 需调速度 PI 时应通过厂家工具调电机内部参数，本工程没有对应 API。

#### Motion 模式

当前大臂轨迹以 `Tff=0 N·m`、`Kp=30`、`Kd=0.5` 作为保守起点。调参顺序与达妙原生 MIT 相同：先对齐当前位置、保持 `W=0/T=0`，从低 Kp/Kd 起步，先升 Kp、再加 Kd，最后加重力/摩擦力矩前馈。同样需要对目标位置做轨迹限制，并另加符合 X4-36 额定和峰值能力的合成力矩保护。

---

## 5. 灵足 RobStride

### 5.1 当前工程状态

灵足命令邮箱默认模式是 `ROBSTRIDE_CTRL_NATIVE_MIT`，默认型号是 RS01/O1，默认 ID 为 1。出处：[Task.c:28](./TASK/src/Task.c#L28)、[robstride_comm_port.h:15](./App/inc/robstride_comm_port.h#L15)、[robstride_comm_port.h:17](./App/inc/robstride_comm_port.h#L17)。

邮箱初始值没有将 `enabled` 设为 1，所以“默认模式”只表示下次使能时选择的模式，不表示上电自动运动。

### 5.2 参数作用与量纲

| 参数 | 量纲 | 作用 | 出处 |
| --- | --- | --- | --- |
| `Pos` / `position` | rad | 原生 MIT 或软件位置环目标 | [RobStride_Motor.h:190](./Motor/inc/RobStride_Motor.h#L190) |
| `W` / `speed` | rad/s | 原生 MIT 或软件速度环目标 | [RobStride_Motor.h:191](./Motor/inc/RobStride_Motor.h#L191) |
| `T` / `torque` | N·m | 原生 MIT 前馈力矩；软件速度环输出 | [RobStride_Motor.h:192](./Motor/inc/RobStride_Motor.h#L192) |
| `K_P` / `kp` | 约 N·m/rad | 原生 MIT 位置刚度 | [RobStride_Motor.h:193](./Motor/inc/RobStride_Motor.h#L193) |
| `K_W` / `kd` | 约 N·m/(rad/s) | 原生 MIT 速度阻尼；软件位置模式的电机内环增益 | [RobStride_Motor.h:194](./Motor/inc/RobStride_Motor.h#L194) |
| `rx_Temp` | °C | 电机温度反馈 | [RobStride_Motor.c:703](./Motor/src/RobStride_Motor.c#L703) |

### 5.3 型号量化范围

| 型号 | 位置 rad | 速度 rad/s | 力矩 N·m | Kp | Kd | 出处 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| RS00 | ±12.566 | ±33 | ±14 | 0～500 | 0～5 | [RobStride_Motor.c:44](./Motor/src/RobStride_Motor.c#L44) |
| RS01/O1 | ±12.566 | ±44 | ±17 | 0～500 | 0～5 | [RobStride_Motor.c:70](./Motor/src/RobStride_Motor.c#L70) |
| RS05 | ±12.566 | ±50 | ±5.5 | 0～500 | 0～5 | [RobStride_Motor.c:57](./Motor/src/RobStride_Motor.c#L57) |
| EL05 | ±12.566 | ±50 | ±6 | 0～500 | 0～5 | [RobStride_Motor.c:84](./Motor/src/RobStride_Motor.c#L84) |

所有量都使用 16 bit 量化，不同型号必须使用对应的速度/力矩范围，否则同一个整数会被解释为不同物理值。出处：[RobStride_Motor.h:24](./Motor/inc/RobStride_Motor.h#L24)、[RobStride_Motor.c:299](./Motor/src/RobStride_Motor.c#L299)。

### 5.4 控制模式与公式

| 模式 | 有效输入 | 控制链/公式 | 出处 |
| --- | --- | --- | --- |
| `ROBSTRIDE_CTRL_STOP` | 无 | 应用层发送一次 Disable，后续保持总线安静 | [robstride_comm_port.c:45](./App/src/robstride_comm_port.c#L45) |
| `ROBSTRIDE_CTRL_NATIVE_MIT` | Pos、W、T、Kp、Kd | `T_cmd = Kp*(q_des-q_actual) + Kd*(w_des-w_actual) + T_ff` | [RobStride_Motor.c:281](./Motor/src/RobStride_Motor.c#L281) |
| `ROBSTRIDE_CTRL_POS` | Pos、`PID.PosPID`、Kd | STM32：`w_des = PID_pos(q_des-q_actual)`；电机：`T_cmd = Kd*(w_des-w_actual)`。发送时 Kp 被强制为 0 | [RobStride_Motor.c:443](./Motor/src/RobStride_Motor.c#L443)、[RobStride_Motor.c:498](./Motor/src/RobStride_Motor.c#L498) |
| `ROBSTRIDE_CTRL_SPD` | W、`PID.SpeedPID` | STM32：`T = PID_spd(w_des-w_actual)`；发送时电机内部 Kp=Kd=0 | [RobStride_Motor.c:450](./Motor/src/RobStride_Motor.c#L450)、[RobStride_Motor.c:503](./Motor/src/RobStride_Motor.c#L503) |

电机还没有进入 `RUN` 状态时，驱动只发 Enable，不会直接发控制帧。出处：[RobStride_Motor.c:490](./Motor/src/RobStride_Motor.c#L490)。

### 5.5 灵足默认软件 PID

| PID | Kp | Ki | Kd | LimitOutput | LimitIntegral | 输出量纲 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| `PosPID` | 5.0 | 0 | 0 | 10.0 | 2.0 | rad/s |
| `SpeedPID` | 0.1 | 0.01 | 0 | 5.0 | 1.0 | N·m |

出处：[RobStride_Motor.c:23](./Motor/src/RobStride_Motor.c#L23)。

这些是工程默认值，不是针对任意机械负载都经过验证的厂家推荐值。且 PID 周期字段没有在 `RobStrideMotor_ConfigType()` 中显式设置，会回退到 10 ms，与实际 1 ms 调用周期不一致；使用 Ki/Kd 前应先配置周期。

### 5.6 灵足分模式调参方法

#### 原生 MIT

1. 确认型号是 RS00、RS01、RS05 还是 EL05，否则速度和力矩编解码会错误。
2. 使能前将 `Pos` 对齐 `rx_Pos`，令 `W=0`、`T=0`。
3. 从低 Kp/Kd 开始，用小位置阶跃验证方向、零点和反馈。
4. 先增 Kp 到所需刚度，再增 Kd 抑制超调，最后增加重力/摩擦前馈 T。
5. 以当前型号的力矩范围和实际机械安全范围限制合成力矩，不要只检查单个 T 字段。

#### 软件位置环 `ROBSTRIDE_CTRL_POS`

1. 命令里的 `kd` 是电机内部速度阻尼，必须先给一个安全的非零值；否则发送时 Kp=0、T=0，目标速度无法产生力矩。
2. 将 `PosPID.Ki=0`、`Kd=0`，从小的 `PosPID.Kp` 开始；`LimitOutput` 是最大目标速度，初期应明显低于协议速度上限。
3. 先调整电机内环 `kd` 使速度跟踪稳定，再增大外环 `PosPID.Kp`。
4. 有轻微稳态误差时，先考虑负载前馈；如果必须使用 Ki，应保持很小并限制 `LimitIntegral`。
5. 设置 `PosPID.flag.ActualPeriodMs=1`，同时设定与调参基准一致的 `BasePeriodMs`。

#### 软件速度环 `ROBSTRIDE_CTRL_SPD`

1. 发送时电机 MIT Kp/Kd 都被置零，STM32 的 `SpeedPID` 直接输出力矩 T。
2. 先令 Ki=Kd=0，从小的速度 Kp 开始，用低速阶跃调到能稳定跟踪。
3. 逐步加小量 Ki 消除带载稳态误差，一旦出现积分饱和、松开负载后反向冲击，应减小 Ki 或 `LimitIntegral`。
4. `SpeedPID.LimitOutput` 是力矩限幅，应按型号和机械容许力矩设置；默认为 5 N·m，对 RS05 已接近代码的 5.5 N·m 量化上限。
5. 设置 `SpeedPID.flag.ActualPeriodMs=1`，再根据是否需要抑制高频噪声决定是否加 D 项和 D 滤波。

---

## 6. 建议的通用安全调参流程

1. 使机构空载或尽量减载，设置物理急停，禁止人员进入运动范围。
2. 确认电机型号、CAN ID、零点、方向、减速比和协议映射范围。
3. 使能前使目标位置与当前反馈位置一致。
4. 限制目标位置增量、目标速度、合成力矩、连续电流、峰值电流和温度。
5. 调级联环时严格按“电流/力矩内环 → 速度环 → 位置环”的顺序，内环带宽应高于外环。
6. 每次只修改一个参数，记录位置、速度、力矩/电流、温度、超调和稳定时间。
7. 调参完成后用最坏负载、最坏姿态和正反转方向复验，不能只在空载单方向下确认。

## 7. 工程当前值中需要优先注意的问题

1. 达妙动作的 `Kp=30/Kd=1` 不区分电机型号，同一组值会用于 J4340 或切换后的 G6220；应建立按型号和机械负载选择的参数表。
2. 达妙 MIT 和脉塔 Motion 只限制单个命令字段，没有限制 MIT 合成力矩。
3. 脉塔 TORQUE/SPEED/POSITION 的任务校验上限是协议整数编码上限，不能作为机械保护值。
4. 达妙软件 PID 尚未设置默认增益和输出限幅，选择 `MIT_POS`/`MIT_POS_SPD` 前必须初始化。
5. 灵足虽然有默认软件 PID，但周期参数仍是 10 ms 回退值，与 1 ms 任务不一致。
6. 当前动作完成判定只检查位置误差，建议增加速度门限和连续稳定时间，避免电机在高速穿过目标时被误判为完成。
